#!/bin/sh

# this script is used to give the user basic capabilities before installing
# (proper keyboard layout, config edition/fetching tools, internet access,...)
# and prepare dependencies for the installer



setup-keymap
# -c: community, -1: first mirror (the default one)
setup-apkrepos -c -1

